{
  "name": "quka",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "preview": "vite preview",
    "tauri": "tauri",
    "tauri:build": "tauri build",
    "tauri:build:windows": "tauri build --target x86_64-pc-windows-msvc --bundles nsis,msi",
    "tauri:build:macos": "tauri build --target universal-apple-darwin --bundles dmg",
    "tauri:build:macos-arm": "tauri build --target aarch64-apple-darwin --bundles dmg",
    "tauri:build:macos-intel": "tauri build --target x86_64-apple-darwin --bundles dmg"
  },
  "dependencies": {
    "@tauri-apps/api": "^2",
    "pinia": "^3",
    "vue": "^3",
    "vue-router": "^4"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "@vitejs/plugin-vue": "^5",
    "typescript": "~5.6.3",
    "vite": "^6",
    "vue-tsc": "^2.2.0"
  }
}
